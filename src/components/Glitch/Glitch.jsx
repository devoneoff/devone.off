import React from "react";
import './Glitch.css';

const Glitch = () => {
    return (
        <div className="glitch-container">
            <div class="glitch-wrapper">
                <div class="glitch" data-glitch="DEVONE.OFF">DEVONE.OFF</div>
            </div>
        </div>
    );
};

export default Glitch;